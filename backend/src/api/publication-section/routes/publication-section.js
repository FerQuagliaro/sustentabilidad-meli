'use strict';

/**
 * publication-section router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter(
  'api::publication-section.publication-section'
);
