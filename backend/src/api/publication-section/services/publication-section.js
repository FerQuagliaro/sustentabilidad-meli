'use strict';

/**
 * publication-section service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService(
  'api::publication-section.publication-section'
);
